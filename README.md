Artifacts Client
================

```
docker build -t artifacts-client . && docker run -e API_TOKEN --rm -it artifacts-client
```

```
docker compose up -d
docker compose --progress plain up -d --build
```
